import { ScaffoldPage } from '@/components/scaffold-page';

export default function StrategiesPage(): JSX.Element {
  return (
    <ScaffoldPage
      title="Strategies"
      description="Configure which strategies are active, weights, and risk limits."
    />
  );
}
