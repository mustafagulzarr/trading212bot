import { ScaffoldPage } from '@/components/scaffold-page';

export default function PositionsPage(): JSX.Element {
  return (
    <ScaffoldPage
      title="Positions"
      description="Open positions, exposure, and P&L will appear here once wired to live portfolio data."
    />
  );
}
