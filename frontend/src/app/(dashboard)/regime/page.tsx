import { ScaffoldPage } from '@/components/scaffold-page';

export default function RegimePage(): JSX.Element {
  return (
    <ScaffoldPage
      title="Regime"
      description="Deep dive into current and historical strategy regime signals."
    />
  );
}
