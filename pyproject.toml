[project]
name = "trading212bot"
version = "0.1.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
  "libs/py_common",
  "backend/market_data_service",
  "backend/trading_service",
  "backend/strategy_service",
]
